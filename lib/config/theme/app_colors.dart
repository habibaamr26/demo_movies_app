
import 'package:flutter/material.dart';

class AppColors {
  // light theme colors
  static const primaryColor = Color(0xFF514eb6);
  static const secondaryColor = Colors.grey;
  static const tertiaryColor = Color(0xFF5d58a0);
  static const backgroundColor = Color(0xFFfefefe);
  static const onSurface = Color(0xFF000000);
  static const cardColor = Color(0xFFf7f2fa);

  // dark theme colors
static const primaryColorK = Color(0xFF8C88FF);      // بنفسجي فاتح يدي لمسة قوية فوق الخلفية الداكنة
static const secondaryColorK = Color(0xFF9E9E9E);    // رمادي متوسط للعناصر الثانوية
static const tertiaryColorK = Color(0xFF5C5C9B);     // بنفسجي غامق قريب من الأساسي
static const backgroundColorK = Color(0xFF121212);   // الخلفية الرئيسية (أسود رمادي داكن جدًا)
static const onSurfaceK = Color(0xFFE0E0E0);         // لون النصوص فوق الخلفية (فاتح)
static const cardColorK = Color(0xFF1E1E2C);         // لون الكروت (غامق بس أفتح من الخلفية)

}
